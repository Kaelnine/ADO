<Window x:Class="TaskTrackingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTrackingApp"
        mc:Ignorable="d"
        Title="Менеджер задач" Height="600" Width="800" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen" WindowState="Maximized" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu DockPanel.Dock="Top" Grid.Row="0">
            <MenuItem Header="_Администрирование">
                <MenuItem Header="_Добавить">
                    <MenuItem Header="_Руководителя" Name="addManager" Click="AddManager_Click"/>
                    <MenuItem Header="_Сотрудника"/>
                </MenuItem>
                <MenuItem Header="_Изменить">
                    <MenuItem Header="_Руководителя"/>
                    <MenuItem Header="_Сотрудника"/>
                </MenuItem>
                <MenuItem Header="_Удалить">
                    <MenuItem Header="_Руководителя"/>
                    <MenuItem Header="_Сотрудника"/>
                </MenuItem>
            </MenuItem>
            
        </Menu>
        <TextBlock Grid.Row="1" Text="Список задач" FontSize="16" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>
        <DataGrid x:Name="taskDataGrid" Grid.Row="2" Margin="10" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер п/п" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Название задачи" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Описание задачи" Binding="{Binding Description}" Width="Auto"/>
                <DataGridTextColumn Header="Статус задачи" Binding="{Binding IdStatus}" Width="Auto"/>
                <DataGridTextColumn Header="Руководитель" Binding="{Binding IdManager}" Width="Auto"/>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding IdStaff}" Width="Auto"/>
                <DataGridTextColumn Header="Дата назначения" Binding="{Binding AssignmentDate}" Width="Auto"/>
                <DataGridTextColumn Header="Срок выполнения" Binding="{Binding PeriodExecution}" Width="Auto"/>
                <DataGridTextColumn Header="Дата завершения" Binding="{Binding CompletionDate}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
            <Button Content="Добавить задачу" Margin="10" HorizontalAlignment="Center" Width="100" Height="20"/>
            <Button Content="Изменить задачу" Margin="10" HorizontalAlignment="Center" Width="100" Height="20"/>
            <Button Content="Удалить задачу" Margin="10" HorizontalAlignment="Center" Width="100" Height="20"/>
            <Button Content="Обновить список" Margin="10" HorizontalAlignment="Center" Width="100" Height="20"/>            
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
            <Button Content="Список задач сотрудника" Margin="10" HorizontalAlignment="Center" Width="180" Height="20"/>
            <Button Content="Список задач руководителя" Margin="10" HorizontalAlignment="Center" Width="180" Height="20"/>
            <Button Content="Список общих задач" Margin="10" HorizontalAlignment="Center" Width="180" Height="20"/>
        </StackPanel>
        <StatusBar Grid.Row="5" Background="#FFD6D3D3">
            <StackPanel Orientation="Horizontal" Margin="10,0" HorizontalAlignment="Center">
                <StatusBarItem Margin="5,0">
                    <TextBlock Text="Информация: "/>
                </StatusBarItem>
                <StatusBarItem Margin="5,0">
                    <TextBlock x:Name="infoTextBlock" Text="Какая то информация"/>
                </StatusBarItem>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0" HorizontalAlignment="Center">
                <StatusBarItem Margin="5,0">
                    <TextBlock Text="Дата:"/>
                </StatusBarItem>
                <StatusBarItem Margin="5,0">
                    <TextBlock x:Name="dateTextBlock" Text="00-00-0000"/>
                </StatusBarItem>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10,0" HorizontalAlignment="Center">

                <StatusBarItem Margin="5,0">
                    <TextBlock Text="Время"/>
                </StatusBarItem>
                <StatusBarItem Margin="5,0">
                    <TextBlock x:Name="timeTextBlock" Text="00:00:00"/>
                </StatusBarItem>
            </StackPanel>
            
        </StatusBar>
    </Grid>
</Window>
